$(document).ready(function() {

// ('<%= @resp.first %>').forEach(function(book){
//   $('body').append(book.title)
//   $('body').append(book.authors)
// });

<% @resp.each do |book| %>
$('body').append(book[:title])
<% end %>

  // $('body').append('<%= @resp.first.title %>');
}) //end of document.ready
