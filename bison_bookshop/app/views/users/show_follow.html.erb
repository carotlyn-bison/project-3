<% provide(:title, @title) %>
<div class="user-info">
  <h1><%= @user.email %></h1>
  <%= link_to "Profile", user_path(@user) %>
  <%= render "stats" %>
</div>
<% if @users.any? %>
<div class="follow-list">
  <% @users.each do |user| %>
  <%= user.email %>
  <% end %>
</div>
<% end %>
